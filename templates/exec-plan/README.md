# ExecPlan template

Use `programmers-loop exec-plan create --help` to generate a valid plan
scaffold for an Assignment or Program. Replace every placeholder and grill the
plan before execution. `programmers-loop exec-plan lint --path <plan> --ready`
must pass before grill, execution, or validation.
