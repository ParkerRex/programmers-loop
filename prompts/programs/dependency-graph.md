# Build a Program dependency graph

Turn converged decisions into a dependency graph. Name each capability or
slice, its prerequisites, outputs, affected interfaces, verification boundary,
and critical ordering constraints. Distinguish a true dependency from a useful
but optional sequence.
