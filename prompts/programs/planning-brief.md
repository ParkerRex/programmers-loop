# Publish a Program planning brief

Synthesize only converged, reviewed decisions into the next immutable
`planning-brief-<N>.md`. Include the decision, bounded next slice, dependencies,
interfaces, non-goals, acceptance, and known risks. Supersede the prior brief
and update `current.txt` atomically only after the new brief is complete.
