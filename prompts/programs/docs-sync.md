# Synchronize docs after a Program slice

Inspect the completed child ExecPlan and implementation diff. Update only the
durable Program and repository docs made stale by the slice. Record the slice
ledger entry, new discoveries, changed interfaces, and remaining work. Do not
use docs to claim behavior that deterministic proof did not establish.
