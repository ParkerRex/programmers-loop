# Run a Program research pass

Investigate one explicit question independently. Separate evidence from
inference, record source locations, confidence, unresolved conflicts, and what
would change the recommendation. Write one `research-pass-<slug>.md` artifact.
