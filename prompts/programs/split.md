# Recommend Program ExecPlan slices

Split the converged work into the smallest independently verifiable ExecPlans.
For each slice, state purpose, exact scope, dependencies, interfaces, acceptance
commands, recovery, and what becomes possible afterward. Avoid horizontal
layers that cannot prove useful behavior on their own.
