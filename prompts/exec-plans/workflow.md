# Run the complete ExecPlan workflow

Move one bounded slice through outline, write, lint, grill, execute, proof, and
validation. Persist each transition and stop on a failed contract, unanswered
owner decision, denied execution consent, rejected command, exhausted repair
budget, or failed acceptance proof. Never convert an agent's claim into proof;
use the deterministic proof receipt.
