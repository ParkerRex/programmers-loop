# Validate an ExecPlan

Inspect the implementation against the plan and its acceptance criteria. Run
only explicitly approved commands through the configured allowlist, repository
working-directory boundary, and timeout. Repair implementation defects within
scope, rerun the narrowest failing proof, and record honest outcomes and
remaining risks.
