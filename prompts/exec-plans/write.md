# Write an ExecPlan

Read the ExecPlan contract, owning Assignment, and immutable planning brief when
present. Write a self-contained plan at the requested path. Separate discovery
from execution, expose every decision, name exact files and interfaces, define
recovery, and include deterministic test commands. Run planning lint and repair
all contract failures before returning.
