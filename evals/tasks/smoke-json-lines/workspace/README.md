# json-lines

Small dependency-free helpers for reading and writing JSON Lines (`.jsonl`)
content. `json-lines.mjs` documents the parsing contract; `index.mjs` is a
tiny demo of both exports.

## Tests

```bash
node --test
```
